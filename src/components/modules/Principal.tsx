"use client";
import { FunctionComponent } from "react";

const Principal: FunctionComponent = (): JSX.Element => {
  return <div className="relative w-full h-[80rem] flex items-start justify-start bg-black">
    
  </div>;
};

export default Principal;
