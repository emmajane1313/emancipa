import { FunctionComponent } from "react";

const Principal: FunctionComponent = (): JSX.Element => {
  return <div className="relative w-full h-[60rem] flex items-start justify-start">
    
  </div>;
};

export default Principal;
